
import { observer } from 'mobx-react';
import { FC } from 'react';
import FirstSection from '../components/FirstSection';

const MainPage: FC = observer(() => {

	return (
		<div>
			{/* <FirstSection FirstArt={<div/>} /> */}
			
			
		</div>
	);
})

export default MainPage;
